import HeroSection from '@/pages/public/homepage/components/HeroSection';

interface Props {
    sampleMcqs: any;
}

const Homepage = ({ sampleMcqs }: Props) => {
    return (
        <>

        </>
    );
};

export default Homepage;
